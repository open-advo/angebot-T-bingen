# angebot-T-bingen

Dieses Repository enthält ein LaTeX-Angebotsdokument für die Weiterentwicklung und Pilotierung von KIWo 2.0 mit der Stadt Tübingen.

## Build

```bash
pdflatex -interaction=nonstopmode -halt-on-error main.tex
```

Damit wird `main.pdf` erzeugt.
